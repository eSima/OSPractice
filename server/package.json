{
  "name": "server",
  "version": "1.0.0",
  "description": "server part OSPractice",
  "main": "src/index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "nodemon --ext js --watch src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.14.10"
  }

}
